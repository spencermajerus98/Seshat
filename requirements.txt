# Seshat — runtime dependencies (all local; no cloud services).
# The frontend is a prebuilt static bundle (frontend/dist), so Node.js is NOT
# required at runtime — only Python.

# Web layer
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.0

# Document parsing / data
python-docx>=1.1
openpyxl>=3.1
pandas>=2.0
pypdf>=4.0
watchdog>=3.0
cryptography>=42.0

# Optional: high-fidelity DOCX→PDF protocol viewing on Windows via Word COM
# automation. If absent (or on non-Windows without LibreOffice), the viewer
# falls back to the client-side docx-preview renderer. See core/docx_pdf.py.
pywin32>=306; sys_platform == "win32"

# Encryption-at-rest backend. Provides the `sqlcipher3` module via a
# prebuilt wheel — no C compiler needed. `sqlcipher3-wheels` ships wheels for
# Windows, macOS and Linux (unlike `sqlcipher3-binary`, which is Linux-only).
# If this fails to install, Seshat still runs but WITHOUT database encryption
# (it warns loudly). See README "Security" for details.
sqlcipher3-wheels>=0.5.7
